# States for ConversationHangler
STATE_RECEIVE_MSG, \
STATE_RECEIVE_PWD, \
STATE_CONFIRM_PWD = range(3)

# Chat_data keys
MSGIDS = 'msg_ids'
SETPWD = 'set_pwd'
MODE = 'mode'
PASSWORD = 'password'

# Mode values
MODE_ENCODE, \
MODE_DECODE = range(2)